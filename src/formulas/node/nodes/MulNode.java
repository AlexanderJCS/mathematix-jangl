package formulas.node.nodes;

import jangl.coords.WorldCoords;

public class MulNode extends Node {
        public MulNode(WorldCoords pos) {
            super(pos, 2, 1, "Mul", 4, null);
        }
}
