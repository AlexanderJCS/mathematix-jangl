package formulas.node.nodes;

import jangl.coords.WorldCoords;

public class SubNode extends Node {
        public SubNode(WorldCoords pos) {
            super(pos, 2, 1, "Sub", 4, null);
        }
}
